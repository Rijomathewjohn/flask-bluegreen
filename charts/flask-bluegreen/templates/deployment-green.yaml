apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "flask.fullname" . }}-green
  labels:
    app: {{ include "flask.fullname" . }}
    color: green
spec:
  replicas: {{ .Values.replicas.green }}
  selector:
    matchLabels:
      app: {{ include "flask.fullname" . }}
      color: green
  template:
    metadata:
      labels:
        app: {{ include "flask.fullname" . }}
        color: green
    spec:
      containers:
        - name: app
          image: "{{ .Values.image.repository }}:{{ .Values.image.tagGreen }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env:
            - name: COLOR
              value: "green"
            - name: VERSION
              value: "{{ .Values.image.tagGreen }}"
          ports:
            - containerPort: 5000
          readinessProbe:
            httpGet:
              path: /healthz
              port: 5000
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /healthz
              port: 5000
            initialDelaySeconds: 5
            periodSeconds: 10

